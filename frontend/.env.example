# Frontend environment configuration (Vite).
# Base URL for the backend API. Defaults to "/api/v1" (served via the dev proxy or a
# same-origin reverse proxy in production). Set this to point at an external API host.
# VITE_API_BASE=/api/v1
